services:
  - type: web
    name: mission-control
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENCLAW_WORKSPACE
        value: /opt/render/project/src/workspace-data
